# .travis.yml
 
language:
  php
 
sudo:
  required
 
services:
  docker 

before_install:
  - docker --version
  - sudo /etc/init.d/mysql stop

install:
  - docker-compose --version
  - docker-compose up -d

#need to fix this
before_script:
  

script:
  # Run your tests here.
  - echo 'Tested'
  - docker ps

after_success:
  - echo 'Got to the end!'