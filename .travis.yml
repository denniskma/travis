# .travis.yml
 
language:
  php
 
sudo:
  required
 
services:
  docker 

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - docker --version

install:
  - docker-compose --version

script:
  # Run your tests here.
  - echo 'Tested'
  - docker ps

after_success:
  - echo 'Got to the end!'